var View_Grid = function(){
  this.view = null;
  this.ctx = null;
  this.init = function(view){
    this.view = view;
    this.ctx = view.ctx;
  };
  this.render = function()
  {
    // TODO: Render the grid.
  };
}
